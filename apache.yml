---
- name: This sets up an httpd webserver
  hosts: localhost
  tasks:
  - name: Install apache packages 
    become: true
    become_method: sudo
    yum:
      name: httpd
      state: present
  - name: ensure httpd is running
    service:
      name: httpd 
      state: started
